import 'package:flutter/material.dart';

class Settingpage_Bottom_navigationbar extends StatelessWidget {
  const Settingpage_Bottom_navigationbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: const Center(
        child: Text('Second Page'),
      ),
    );
  }
}
