import 'package:flutter/material.dart';

class Profilepage_Bottom_navigationbar extends StatelessWidget {
  const Profilepage_Bottom_navigationbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: const Center(
        child: Text('Third Page'),
      ),
    );
  }
}
